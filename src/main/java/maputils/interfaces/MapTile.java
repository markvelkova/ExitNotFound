package maputils.interfaces;

/**
 * Interface representing a single tile on the game map.
 * Map tiles are the basic grid elements of the world. All map tiles are discoverable,
 * meaning players can explore and reveal them as they move through the world. */
public interface MapTile extends Discoverable{
}