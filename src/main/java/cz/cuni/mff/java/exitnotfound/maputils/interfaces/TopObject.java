package cz.cuni.mff.java.exitnotfound.maputils.interfaces;

import java.io.Serializable;


/**
 * Marker interface for objects that exist as interactive elements on top of map tiles.
 * Top objects (apples, doors, etc.) are separate entities that can exist at the same
 * location as tiles. They provide special interactions when the player encounters them. */
public interface TopObject {
}